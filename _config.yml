title: "Hydra User's Guide"
description: "Comprehensive documentation for the Hydra project."

remote_theme: "just-the-docs/just-the-docs@v0.5.4"

# Just the Docs settings
just_the_docs:
  skin:
    color_scheme: "light"

# Exclude files from processing
exclude:
  - Gemfile
  - Gemfile.lock
  - node_modules
  - vendor

# Build settings
markdown: kramdown

logo: "/assets/hydra_logo.png"

# In _config.yml
# Color scheme
color_scheme: "dark"

# Custom colors
just_the_docs:
  palette:
    primary:
      hue: "blue"
      saturation: "60%"
      lightness: "50%"
    accent:
      hue: "red"
      saturation: "60%"
      lightness: "50%"